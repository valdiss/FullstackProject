imports:
    - { resource: config.yml }

#doctrine:
#    orm:
#        metadata_cache_driver: apc
#        result_cache_driver: apc
#        query_cache_driver: apc
    dbal:
      url: '%mysql://env(DATABASE_USER):env(DATABASE_PASSWORD)@env(DATABASE_HOST):env(DATABASE_PORT)/env(DATABASE_NAME)?charset=UTF-8%'

monolog:
    handlers:
        main:
            type: fingers_crossed
            action_level: error
            handler: nested
        nested:
            type: stream
            path: 'php://stderr'
            level: debug
        console:
            type: console
